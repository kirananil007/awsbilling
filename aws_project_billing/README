Requirements :: 

1) Python 2.7.6

2) Django 1.9.1
	pip install Django==1.9.1

3) Install virtualenv

4) Install git

5) install Python pip

6) install libmysqlclient-dev 

Steps for importing from csv to MySQL database::

	mysql -uroot -p --local-infile

	use awsbilling;

	LOAD DATA LOCAL INFILE '/path/.csv' INTO TABLE tablename FIELDS TERMINATED BY '|';